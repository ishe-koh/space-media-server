# Vision Template

このディレクトリは新規ビジョン構成のテンプレートです。

- `config/vision_config.json`: cabinet/screen/lanes の定義
- `source/media/`: 素材置き場（動画・画像）
- `source/playlists/`: server用playlist（`source`指定）

新規作成:
```
./tools/init_vision.py <vision_id>
```
